---
interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const { title, description = "The only outbound agency built exclusively for supply chain, logistics, and procurement software.", ogImage = "/og/default.png" } = Astro.props;
const currentPath = Astro.url.pathname;
const ogImageUrl = ogImage.startsWith("http") ? ogImage : `https://lynnleadgen.com${ogImage}`;
import '../styles/global.css';
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-light-32.png" media="(prefers-color-scheme: light)" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-dark-32.png" media="(prefers-color-scheme: dark)" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-light-32.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon-light-180.png" />
    <link rel="canonical" href={`https://lynnleadgen.com${currentPath}`} />

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={`https://lynnleadgen.com${currentPath}`} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:site_name" content="Lynn Lead Gen" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Lexend+Deca:wght@100..900&display=swap" rel="stylesheet" />

    <!-- Google Analytics 4 -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-Z0069897K2"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-Z0069897K2', { anonymize_ip: true });
    </script>

    <style is:global>
      html {
        scroll-behavior: smooth;
        scroll-padding-top: 100px;
      }
      body {
        background-color: #F9F8F4;
        color: #1a1a1a;
      }
      ::selection {
        background-color: #C5A059;
        color: #ffffff;
      }
      @keyframes scroll-logos {
        0% { transform: translateX(0); }
        100% { transform: translateX(-50%); }
      }
      .animate-scroll-logos {
        animation: scroll-logos 75s linear infinite;
      }
      .animate-scroll-logos:hover {
        animation-play-state: paused;
      }
    </style>
  </head>
  <body class="min-h-screen bg-[#F9F8F4] text-[#1a1a1a] selection:bg-[#C5A059] selection:text-white font-sans">
    <div id="nav-root">
      <script define:vars={{ currentPath }}>
        window.__CURRENT_PATH__ = currentPath;
      </script>
    </div>

    <main>
      <slot />
    </main>

    <div id="footer-root"></div>
    <div id="cookie-root"></div>

    <!-- Mount React nav, footer, and cookie banner -->
    <script>
      import React from 'react';
      import ReactDOM from 'react-dom/client';
      import Nav from '../components/Nav';
      import Footer from '../components/Footer';
      import { CookieBanner } from '../components/CookieBanner';

      const currentPath = (window as any).__CURRENT_PATH__ || '/';

      const navEl = document.getElementById('nav-root');
      if (navEl) {
        ReactDOM.createRoot(navEl).render(React.createElement(Nav, { currentPath }));
      }

      const footerEl = document.getElementById('footer-root');
      if (footerEl) {
        ReactDOM.createRoot(footerEl).render(React.createElement(Footer));
      }

      const cookieEl = document.getElementById('cookie-root');
      if (cookieEl) {
        ReactDOM.createRoot(cookieEl).render(React.createElement(CookieBanner));
      }
    </script>
  </body>
</html>
